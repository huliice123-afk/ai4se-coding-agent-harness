services:
  - type: web
    name: coding-agent-harness
    runtime: docker
    dockerfilePath: ./web/Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
    healthCheckPath: /
    autoDeploy: true